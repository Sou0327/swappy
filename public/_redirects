# SPA (Single Page Application) 向けリダイレクト設定
# Cloudflare Pages で使用される _redirects ファイル
#
# React Router のクライアントサイドルーティングに対応するため、
# 存在しないパスへのリクエストを index.html にリダイレクトする

# 静的アセットは除外（CSS, JS, 画像など）
/assets/*  /assets/:splat  200

# その他の全てのリクエストを index.html にリダイレクト
/*  /index.html  200
